

/*const favoriteIds = [101, 205, 309, 412];
const currentId = 2005;

const answer = favoriteIds.includes(currentId) ?  "товар в избранном" : "Товара нет в избранном";

console.log(answer);*/




/*const tasks = ['Поесть', 'Позвонить', 'Погулять', 'Почитать'];

let queryId = tasks.indexOf("Позвонить");

if(queryId !==-1 ) {
    tasks.splice(queryId, 1);
}

console.log();*/


const users = ['user1@mail.com', 'user2@mail.com', 'user3@mail.com'];

users.forEach( element=>{
    console.log(element);
});